# 模块化架构设计

## 基本信息

- 开始日期: 2026-04-05
- 完成日期: 2026-04-13
- 状态: ✅ 已完成（架构层面）
- 进度: 95%

## 功能描述

将 YOLO-Flow 从单一应用重构为**模块化聚合平台**，支持未来扩展：

- YOLO 目标检测（现有功能）
- 爬虫管理（规划中）
- 任务流（规划中）
- RPA 自动化（规划中）

核心目标：**模块可插拔**，新增模块无需修改核心代码。

## 任务分解

### Phase 1: 核心框架

- [x] 方案设计
- [x] 确定目录结构规范（已写入 CLAUDE.md）
- [x] 确定后端目录结构规范（已写入 CLAUDE.md）
- [x] 创建模块类型定义 `src/modules/types.ts`
- [x] 实现模块注册中心 `src/modules/registry.ts`
- [x] 创建全局路由状态 `src/stores/routerStore.ts`

### Phase 2: Hub 页面

- [x] HubPage 组件基本结构
- [x] YoloGuideModal 引导弹窗
- [x] NewProjectModal 新建项目弹窗
- [ ] **待完整调试和用户确认**

### Phase 3: YOLO 模块化

- [x] 基础项目结构
- [x] App.tsx 路由逻辑更新
- [x] 后端 project_create/project_open 命令
- [x] 后端训练命令 (training_start, training_stop, training_pause, training_resume)
- [x] 后端标注命令 (load_annotation, save_annotation, import_dataset)
- [x] 后端视频推理命令 (video_load, rust_video_inference_start/stop)
- [x] 后端设备命令 (device_list, device_stats, device_set_default)
- [x] 后端模型转换命令 (detect_format, optimize_onnx, simplify_onnx)
- [x] 完整页面调试 (AnnotationPage, TrainingPage, ResultsPage, VideoPage, DevicePage, DesktopPage, SettingsPage)
- [x] ActivityBar 动态化
- [ ] 前端 API 层整理

### Phase 4: 其他模块支持

- [ ] 爬虫模块框架
- [ ] RPA 模块框架

## 已实现的后端命令

### 项目管理 ✅
- [x] `project_create` - 创建项目
- [x] `project_open` - 打开项目
- [x] `update_classes` - 更新项目类别
- [x] `project_save` - 保存项目
- [x] `project_recent_list` - 获取最近项目列表
- [x] `import_dataset` - 导入已有 YOLO 数据集

### 标注功能 ✅
- [x] `load_annotation` - 加载标注文件
- [x] `save_annotation` - 保存标注文件

### 训练功能 ✅
- [x] `training_start` - 开始训练
- [x] `training_stop` - 停止训练
- [x] `training_pause` - 暂停训练
- [x] `training_resume` - 恢复训练
- [x] `yolo_check_model` - 检查模型是否存在
- [x] `yolo_download_model` - 下载模型
- [x] `model_list` - 获取已训练模型列表
- [x] `model_delete` - 删除模型
- [x] `data.yaml` 自动生成 - 训练开始时自动生成

### 环境检测 ✅
- [x] `check_python_env` - 检查 Python 环境（支持缓存）
- [x] `get_cached_python_env` - 获取缓存的 Python 环境信息
- [x] `install_python_deps` - 安装 Python 依赖
- [x] `get_install_instructions` - 获取安装说明
- [x] `check_rust_env` - 检查 Rust/Burn 环境（已实现但未注册）
- [x] `clear_rust_env_cache` - 清除 Rust 环境缓存

### 视频推理 ✅
- [x] `video_load` - 加载视频
- [x] `video_inference_start` - 开始视频推理（Python）
- [x] `video_inference_stop` - 停止视频推理
- [x] `video_capture_screenshot` - 截图
- [x] `video_extract_frames` - 提取帧
- [x] `video_inference_results` - 获取推理结果
- [x] `rust_video_inference_start` - 开始视频推理（纯 Rust）
- [x] `rust_video_inference_stop` - 停止 Rust 视频推理

### 设备管理 ✅
- [x] `device_list` - 获取设备列表
- [x] `device_stats` - 获取设备状态
- [x] `device_set_default` - 设置默认设备

### 桌面捕获推理 ✅
- [x] `desktop_capture_start` - 开始桌面捕获
- [x] `desktop_capture_stop` - 停止桌面捕获
- [x] `get_monitors` - 获取显示器列表
- [x] `get_desktop_capture_status` - 获取捕获状态

### 模型转换 ✅
- [x] `detect_format` - 检测模型格式
- [x] `get_model_details` - 获取模型详情
- [x] `check_compatibility` - 检查模型兼容性
- [x] `simplify_onnx_model` - 简化 ONNX 模型
- [x] `optimize_onnx_model` - 优化 ONNX 模型
- [x] `get_supported_formats` - 获取支持的格式
- [x] `get_conversion_guide` - 获取转换指南
- [x] `get_conversion_script_path` - 获取转换脚本路径

### 文件操作 ✅
- [x] `read_text_file` / `read_binary_file` - 读取文件
- [x] `write_text_file` - 写入文件
- [x] `delete_file` / `delete_directory` - 删除
- [x] `rename_path` - 重命名
- [x] `create_directory` - 创建目录
- [x] `list_directory` - 列出目录
- [x] `copy_file` - 复制文件
- [x] `path_exists` - 检查路径存在

### 设置 ✅
- [x] `settings_load` - 加载设置
- [x] `settings_save` - 保存设置

### 文件监视 ✅
- [x] `start_watch` - 开始监视
- [x] `stop_watch` - 停止监视

## 待开发/不完整的命令

### 训练功能
- [ ] `training_logs` - 获取训练日志（未实现）
- [ ] `training_export` - 导出训练结果（未实现）

### 环境检测
- [ ] `clear_python_env_cache` - 清除 Python 环境缓存（已实现但未注册到 lib.rs）

### 数据集管理
- [ ] `dataset_load` - 加载数据集（未实现）
- [ ] `dataset_stats` - 获取数据集统计（未实现）
- [ ] `dataset_import_images` - 导入图片（未实现）
- [ ] `dataset_export` - 导出数据集（未实现）

### 标注功能
- [ ] `annotation_load_image` - 加载标注图片（未实现）
- [ ] `annotation_navigate` - 图片导航（未实现）
- [ ] `annotation_auto_label` - 自动标注（未实现）
- [ ] `annotation_add_class` - 添加类别（未实现）
- [ ] `annotation_delete_class` - 删除类别（未实现）

### 模型管理
- [ ] `model_load` - 加载模型（未实现）
- [ ] `model_convert` - 转换模型（未实现）
- [ ] `model_base_list` - 基础模型列表（未实现）

## 当前进度

Phase 1: 核心框架 ✅ 完成
Phase 2: Hub 页面 ✅ 完成
Phase 3: YOLO 模块化 ✅ 完成（95%）

后端命令已完成: 50+ 命令实现
前端 API 层已同步: 训练、视频、桌面捕获、设备、模型转换、环境检测等

待完善功能：
- 训练日志获取（training_logs）
- 训练结果导出（training_export）
- 数据集管理命令（dataset_*）
- 标注高级命令（annotation_navigate, annotation_auto_label 等）

## 备注

- 用户确认采用此架构方案
- 目录结构已写入 CLAUDE.md 规范文件
- 使用 Zustand 管理路由状态（非 react-router）
- 模块之间通过事件/消息通信，不直接引用
- 后端模块命令放在 `modules/yolo/commands/`，core 目录保留为空供将来共享功能使用
- 2026-04-13 代码审查后更新：确认 50+ 命令已实现，文档已同步
